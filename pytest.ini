[pytest]
pythonpath = backend/src
testpaths = tests
addopts = -ra --strict-config --strict-markers
asyncio_mode = auto
markers =
    integration: PostgreSQL-backed tests
    e2e: browser tests
    security: security controls
    performance: performance budgets
    ai_eval: deterministic AI evaluation
